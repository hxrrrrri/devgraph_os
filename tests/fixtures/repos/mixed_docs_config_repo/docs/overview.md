# Overview

This repository hosts the data ingestion pipeline.

## Architecture

The pipeline reads from S3, transforms with dbt, then writes to Postgres.

## Operations

Runbooks live under `docs/runbooks/`.
